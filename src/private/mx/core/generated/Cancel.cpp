// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/Cancel.h"

#include "mx/core/Lexical.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const std::optional<CancelLocation> &Cancel::location() const noexcept
{
    return m_location;
}

void Cancel::setLocation(std::optional<CancelLocation> value)
{
    m_location = std::move(value);
}

const Fifths &Cancel::value() const noexcept
{
    return m_value;
}

void Cancel::setValue(Fifths value)
{
    m_value = std::move(value);
}

Cancel parseCancel(pugi::xml_node el)
{
    Cancel out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "location")
        {
            out.setLocation(CancelLocation::parse(a.value()));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    parseCancelContent(out, el);
    return out;
}

void parseCancelContent(Cancel &out, pugi::xml_node el)
{
    out.setValue(Fifths::parse(childText(el)));
    if (firstElement(el))
    {
        throwUnknownElement(firstElement(el));
    }
}

void serializeCancel(const Cancel &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    if (v.location())
    {
        setAttribute(el, "location", v.location()->toString());
    }
    serializeCancelContent(v, el);
}

void serializeCancelContent(const Cancel &v, pugi::xml_node el)
{
    setText(el, v.value().toString());
}

} // namespace mx::core
