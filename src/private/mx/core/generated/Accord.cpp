// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/Accord.h"

#include "mx/core/Lexical.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const std::optional<StringNumber> &Accord::string() const noexcept
{
    return m_string;
}

void Accord::setString(std::optional<StringNumber> value)
{
    m_string = std::move(value);
}

const TuningGroup &Accord::tuning() const noexcept
{
    return m_tuning;
}

void Accord::setTuning(TuningGroup value)
{
    m_tuning = std::move(value);
}

Accord parseAccord(pugi::xml_node el)
{
    Accord out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "string")
        {
            out.setString(StringNumber::parse(a.value()));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    parseAccordContent(out, el);
    return out;
}

void parseAccordContent(Accord &out, pugi::xml_node el)
{
    pugi::xml_node cursor = firstElement(el);
    if (cursor && (cursorIs(cursor, "tuning-step")))
    {
        out.setTuning(parseTuningGroup(el, cursor));
    }
    else
    {
        throwMissingElement(el, "tuning-step");
    }
    if (cursor)
    {
        if (cursorIs(cursor, "tuning-step") || cursorIs(cursor, "tuning-alter") || cursorIs(cursor, "tuning-octave"))
        {
            throwMisplacedElement(cursor);
        }
        throwUnknownElement(cursor);
    }
}

void serializeAccord(const Accord &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    if (v.string())
    {
        setAttribute(el, "string", v.string()->toString());
    }
    serializeAccordContent(v, el);
}

void serializeAccordContent(const Accord &v, pugi::xml_node el)
{
    serializeTuningGroup(v.tuning(), el);
}

} // namespace mx::core
