// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/HarmonyAlter.h"

#include "mx/core/Lexical.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const std::optional<LeftRight> &HarmonyAlter::location() const noexcept
{
    return m_location;
}

void HarmonyAlter::setLocation(std::optional<LeftRight> value)
{
    m_location = std::move(value);
}

const std::optional<YesNo> &HarmonyAlter::printObject() const noexcept
{
    return m_printObject;
}

void HarmonyAlter::setPrintObject(std::optional<YesNo> value)
{
    m_printObject = std::move(value);
}

const std::optional<Tenths> &HarmonyAlter::defaultX() const noexcept
{
    return m_defaultX;
}

void HarmonyAlter::setDefaultX(std::optional<Tenths> value)
{
    m_defaultX = std::move(value);
}

const std::optional<Tenths> &HarmonyAlter::defaultY() const noexcept
{
    return m_defaultY;
}

void HarmonyAlter::setDefaultY(std::optional<Tenths> value)
{
    m_defaultY = std::move(value);
}

const std::optional<Tenths> &HarmonyAlter::relativeX() const noexcept
{
    return m_relativeX;
}

void HarmonyAlter::setRelativeX(std::optional<Tenths> value)
{
    m_relativeX = std::move(value);
}

const std::optional<Tenths> &HarmonyAlter::relativeY() const noexcept
{
    return m_relativeY;
}

void HarmonyAlter::setRelativeY(std::optional<Tenths> value)
{
    m_relativeY = std::move(value);
}

const std::optional<FontFamily> &HarmonyAlter::fontFamily() const noexcept
{
    return m_fontFamily;
}

void HarmonyAlter::setFontFamily(std::optional<FontFamily> value)
{
    m_fontFamily = std::move(value);
}

const std::optional<FontStyle> &HarmonyAlter::fontStyle() const noexcept
{
    return m_fontStyle;
}

void HarmonyAlter::setFontStyle(std::optional<FontStyle> value)
{
    m_fontStyle = std::move(value);
}

const std::optional<FontSize> &HarmonyAlter::fontSize() const noexcept
{
    return m_fontSize;
}

void HarmonyAlter::setFontSize(std::optional<FontSize> value)
{
    m_fontSize = std::move(value);
}

const std::optional<FontWeight> &HarmonyAlter::fontWeight() const noexcept
{
    return m_fontWeight;
}

void HarmonyAlter::setFontWeight(std::optional<FontWeight> value)
{
    m_fontWeight = std::move(value);
}

const std::optional<Color> &HarmonyAlter::color() const noexcept
{
    return m_color;
}

void HarmonyAlter::setColor(std::optional<Color> value)
{
    m_color = std::move(value);
}

const Semitones &HarmonyAlter::value() const noexcept
{
    return m_value;
}

void HarmonyAlter::setValue(Semitones value)
{
    m_value = std::move(value);
}

HarmonyAlter parseHarmonyAlter(pugi::xml_node el)
{
    HarmonyAlter out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "location")
        {
            out.setLocation(LeftRight::parse(a.value()));
        }
        else if (aname == "print-object")
        {
            out.setPrintObject(YesNo::parse(a.value()));
        }
        else if (aname == "default-x")
        {
            out.setDefaultX(Tenths::parse(a.value()));
        }
        else if (aname == "default-y")
        {
            out.setDefaultY(Tenths::parse(a.value()));
        }
        else if (aname == "relative-x")
        {
            out.setRelativeX(Tenths::parse(a.value()));
        }
        else if (aname == "relative-y")
        {
            out.setRelativeY(Tenths::parse(a.value()));
        }
        else if (aname == "font-family")
        {
            out.setFontFamily(FontFamily::parse(a.value()));
        }
        else if (aname == "font-style")
        {
            out.setFontStyle(FontStyle::parse(a.value()));
        }
        else if (aname == "font-size")
        {
            out.setFontSize(FontSize::parse(a.value()));
        }
        else if (aname == "font-weight")
        {
            out.setFontWeight(FontWeight::parse(a.value()));
        }
        else if (aname == "color")
        {
            out.setColor(Color::parse(a.value()));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    parseHarmonyAlterContent(out, el);
    return out;
}

void parseHarmonyAlterContent(HarmonyAlter &out, pugi::xml_node el)
{
    out.setValue(Semitones::parse(childText(el)));
    if (firstElement(el))
    {
        throwUnknownElement(firstElement(el));
    }
}

void serializeHarmonyAlter(const HarmonyAlter &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    if (v.location())
    {
        setAttribute(el, "location", v.location()->toString());
    }
    if (v.printObject())
    {
        setAttribute(el, "print-object", v.printObject()->toString());
    }
    if (v.defaultX())
    {
        setAttribute(el, "default-x", v.defaultX()->toString());
    }
    if (v.defaultY())
    {
        setAttribute(el, "default-y", v.defaultY()->toString());
    }
    if (v.relativeX())
    {
        setAttribute(el, "relative-x", v.relativeX()->toString());
    }
    if (v.relativeY())
    {
        setAttribute(el, "relative-y", v.relativeY()->toString());
    }
    if (v.fontFamily())
    {
        setAttribute(el, "font-family", v.fontFamily()->toString());
    }
    if (v.fontStyle())
    {
        setAttribute(el, "font-style", v.fontStyle()->toString());
    }
    if (v.fontSize())
    {
        setAttribute(el, "font-size", v.fontSize()->toString());
    }
    if (v.fontWeight())
    {
        setAttribute(el, "font-weight", v.fontWeight()->toString());
    }
    if (v.color())
    {
        setAttribute(el, "color", v.color()->toString());
    }
    serializeHarmonyAlterContent(v, el);
}

void serializeHarmonyAlterContent(const HarmonyAlter &v, pugi::xml_node el)
{
    setText(el, v.value().toString());
}

} // namespace mx::core
