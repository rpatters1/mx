// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/Offset.h"

#include "mx/core/Lexical.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const std::optional<YesNo> &Offset::sound() const noexcept
{
    return m_sound;
}

void Offset::setSound(std::optional<YesNo> value)
{
    m_sound = std::move(value);
}

const Divisions &Offset::value() const noexcept
{
    return m_value;
}

void Offset::setValue(Divisions value)
{
    m_value = std::move(value);
}

Offset parseOffset(pugi::xml_node el)
{
    Offset out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "sound")
        {
            out.setSound(YesNo::parse(a.value()));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    parseOffsetContent(out, el);
    return out;
}

void parseOffsetContent(Offset &out, pugi::xml_node el)
{
    out.setValue(Divisions::parse(childText(el)));
    if (firstElement(el))
    {
        throwUnknownElement(firstElement(el));
    }
}

void serializeOffset(const Offset &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    if (v.sound())
    {
        setAttribute(el, "sound", v.sound()->toString());
    }
    serializeOffsetContent(v, el);
}

void serializeOffsetContent(const Offset &v, pugi::xml_node el)
{
    setText(el, v.value().toString());
}

} // namespace mx::core
