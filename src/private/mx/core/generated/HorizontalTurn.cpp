// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/HorizontalTurn.h"

#include "mx/core/Lexical.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const std::optional<YesNo> &HorizontalTurn::slash() const noexcept
{
    return m_slash;
}

void HorizontalTurn::setSlash(std::optional<YesNo> value)
{
    m_slash = std::move(value);
}

const std::optional<Tenths> &HorizontalTurn::defaultX() const noexcept
{
    return m_defaultX;
}

void HorizontalTurn::setDefaultX(std::optional<Tenths> value)
{
    m_defaultX = std::move(value);
}

const std::optional<Tenths> &HorizontalTurn::defaultY() const noexcept
{
    return m_defaultY;
}

void HorizontalTurn::setDefaultY(std::optional<Tenths> value)
{
    m_defaultY = std::move(value);
}

const std::optional<Tenths> &HorizontalTurn::relativeX() const noexcept
{
    return m_relativeX;
}

void HorizontalTurn::setRelativeX(std::optional<Tenths> value)
{
    m_relativeX = std::move(value);
}

const std::optional<Tenths> &HorizontalTurn::relativeY() const noexcept
{
    return m_relativeY;
}

void HorizontalTurn::setRelativeY(std::optional<Tenths> value)
{
    m_relativeY = std::move(value);
}

const std::optional<FontFamily> &HorizontalTurn::fontFamily() const noexcept
{
    return m_fontFamily;
}

void HorizontalTurn::setFontFamily(std::optional<FontFamily> value)
{
    m_fontFamily = std::move(value);
}

const std::optional<FontStyle> &HorizontalTurn::fontStyle() const noexcept
{
    return m_fontStyle;
}

void HorizontalTurn::setFontStyle(std::optional<FontStyle> value)
{
    m_fontStyle = std::move(value);
}

const std::optional<FontSize> &HorizontalTurn::fontSize() const noexcept
{
    return m_fontSize;
}

void HorizontalTurn::setFontSize(std::optional<FontSize> value)
{
    m_fontSize = std::move(value);
}

const std::optional<FontWeight> &HorizontalTurn::fontWeight() const noexcept
{
    return m_fontWeight;
}

void HorizontalTurn::setFontWeight(std::optional<FontWeight> value)
{
    m_fontWeight = std::move(value);
}

const std::optional<Color> &HorizontalTurn::color() const noexcept
{
    return m_color;
}

void HorizontalTurn::setColor(std::optional<Color> value)
{
    m_color = std::move(value);
}

const std::optional<AboveBelow> &HorizontalTurn::placement() const noexcept
{
    return m_placement;
}

void HorizontalTurn::setPlacement(std::optional<AboveBelow> value)
{
    m_placement = std::move(value);
}

const std::optional<StartNote> &HorizontalTurn::startNote() const noexcept
{
    return m_startNote;
}

void HorizontalTurn::setStartNote(std::optional<StartNote> value)
{
    m_startNote = std::move(value);
}

const std::optional<TrillStep> &HorizontalTurn::trillStep() const noexcept
{
    return m_trillStep;
}

void HorizontalTurn::setTrillStep(std::optional<TrillStep> value)
{
    m_trillStep = std::move(value);
}

const std::optional<TwoNoteTurn> &HorizontalTurn::twoNoteTurn() const noexcept
{
    return m_twoNoteTurn;
}

void HorizontalTurn::setTwoNoteTurn(std::optional<TwoNoteTurn> value)
{
    m_twoNoteTurn = std::move(value);
}

const std::optional<YesNo> &HorizontalTurn::accelerate() const noexcept
{
    return m_accelerate;
}

void HorizontalTurn::setAccelerate(std::optional<YesNo> value)
{
    m_accelerate = std::move(value);
}

const std::optional<TrillBeats> &HorizontalTurn::beats() const noexcept
{
    return m_beats;
}

void HorizontalTurn::setBeats(std::optional<TrillBeats> value)
{
    m_beats = std::move(value);
}

const std::optional<Percent> &HorizontalTurn::secondBeat() const noexcept
{
    return m_secondBeat;
}

void HorizontalTurn::setSecondBeat(std::optional<Percent> value)
{
    m_secondBeat = std::move(value);
}

const std::optional<Percent> &HorizontalTurn::lastBeat() const noexcept
{
    return m_lastBeat;
}

void HorizontalTurn::setLastBeat(std::optional<Percent> value)
{
    m_lastBeat = std::move(value);
}

HorizontalTurn parseHorizontalTurn(pugi::xml_node el)
{
    HorizontalTurn out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "slash")
        {
            out.setSlash(YesNo::parse(a.value()));
        }
        else if (aname == "default-x")
        {
            out.setDefaultX(Tenths::parse(a.value()));
        }
        else if (aname == "default-y")
        {
            out.setDefaultY(Tenths::parse(a.value()));
        }
        else if (aname == "relative-x")
        {
            out.setRelativeX(Tenths::parse(a.value()));
        }
        else if (aname == "relative-y")
        {
            out.setRelativeY(Tenths::parse(a.value()));
        }
        else if (aname == "font-family")
        {
            out.setFontFamily(FontFamily::parse(a.value()));
        }
        else if (aname == "font-style")
        {
            out.setFontStyle(FontStyle::parse(a.value()));
        }
        else if (aname == "font-size")
        {
            out.setFontSize(FontSize::parse(a.value()));
        }
        else if (aname == "font-weight")
        {
            out.setFontWeight(FontWeight::parse(a.value()));
        }
        else if (aname == "color")
        {
            out.setColor(Color::parse(a.value()));
        }
        else if (aname == "placement")
        {
            out.setPlacement(AboveBelow::parse(a.value()));
        }
        else if (aname == "start-note")
        {
            out.setStartNote(StartNote::parse(a.value()));
        }
        else if (aname == "trill-step")
        {
            out.setTrillStep(TrillStep::parse(a.value()));
        }
        else if (aname == "two-note-turn")
        {
            out.setTwoNoteTurn(TwoNoteTurn::parse(a.value()));
        }
        else if (aname == "accelerate")
        {
            out.setAccelerate(YesNo::parse(a.value()));
        }
        else if (aname == "beats")
        {
            out.setBeats(TrillBeats::parse(a.value()));
        }
        else if (aname == "second-beat")
        {
            out.setSecondBeat(Percent::parse(a.value()));
        }
        else if (aname == "last-beat")
        {
            out.setLastBeat(Percent::parse(a.value()));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    parseHorizontalTurnContent(out, el);
    return out;
}

void parseHorizontalTurnContent(HorizontalTurn &out, pugi::xml_node el)
{
    (void)out;
    if (firstElement(el))
    {
        throwUnknownElement(firstElement(el));
    }
}

void serializeHorizontalTurn(const HorizontalTurn &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    if (v.slash())
    {
        setAttribute(el, "slash", v.slash()->toString());
    }
    if (v.defaultX())
    {
        setAttribute(el, "default-x", v.defaultX()->toString());
    }
    if (v.defaultY())
    {
        setAttribute(el, "default-y", v.defaultY()->toString());
    }
    if (v.relativeX())
    {
        setAttribute(el, "relative-x", v.relativeX()->toString());
    }
    if (v.relativeY())
    {
        setAttribute(el, "relative-y", v.relativeY()->toString());
    }
    if (v.fontFamily())
    {
        setAttribute(el, "font-family", v.fontFamily()->toString());
    }
    if (v.fontStyle())
    {
        setAttribute(el, "font-style", v.fontStyle()->toString());
    }
    if (v.fontSize())
    {
        setAttribute(el, "font-size", v.fontSize()->toString());
    }
    if (v.fontWeight())
    {
        setAttribute(el, "font-weight", v.fontWeight()->toString());
    }
    if (v.color())
    {
        setAttribute(el, "color", v.color()->toString());
    }
    if (v.placement())
    {
        setAttribute(el, "placement", v.placement()->toString());
    }
    if (v.startNote())
    {
        setAttribute(el, "start-note", v.startNote()->toString());
    }
    if (v.trillStep())
    {
        setAttribute(el, "trill-step", v.trillStep()->toString());
    }
    if (v.twoNoteTurn())
    {
        setAttribute(el, "two-note-turn", v.twoNoteTurn()->toString());
    }
    if (v.accelerate())
    {
        setAttribute(el, "accelerate", v.accelerate()->toString());
    }
    if (v.beats())
    {
        setAttribute(el, "beats", v.beats()->toString());
    }
    if (v.secondBeat())
    {
        setAttribute(el, "second-beat", v.secondBeat()->toString());
    }
    if (v.lastBeat())
    {
        setAttribute(el, "last-beat", v.lastBeat()->toString());
    }
    serializeHorizontalTurnContent(v, el);
}

void serializeHorizontalTurnContent(const HorizontalTurn &v, pugi::xml_node el)
{
    (void)v;
    (void)el;
}

} // namespace mx::core
