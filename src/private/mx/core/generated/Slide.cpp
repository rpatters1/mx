// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/Slide.h"

#include "mx/core/Lexical.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const StartStop &Slide::type() const noexcept
{
    return m_type;
}

void Slide::setType(StartStop value)
{
    m_type = std::move(value);
}

const std::optional<NumberLevel> &Slide::number() const noexcept
{
    return m_number;
}

void Slide::setNumber(std::optional<NumberLevel> value)
{
    m_number = std::move(value);
}

const std::optional<LineType> &Slide::lineType() const noexcept
{
    return m_lineType;
}

void Slide::setLineType(std::optional<LineType> value)
{
    m_lineType = std::move(value);
}

const std::optional<Tenths> &Slide::dashLength() const noexcept
{
    return m_dashLength;
}

void Slide::setDashLength(std::optional<Tenths> value)
{
    m_dashLength = std::move(value);
}

const std::optional<Tenths> &Slide::spaceLength() const noexcept
{
    return m_spaceLength;
}

void Slide::setSpaceLength(std::optional<Tenths> value)
{
    m_spaceLength = std::move(value);
}

const std::optional<Tenths> &Slide::defaultX() const noexcept
{
    return m_defaultX;
}

void Slide::setDefaultX(std::optional<Tenths> value)
{
    m_defaultX = std::move(value);
}

const std::optional<Tenths> &Slide::defaultY() const noexcept
{
    return m_defaultY;
}

void Slide::setDefaultY(std::optional<Tenths> value)
{
    m_defaultY = std::move(value);
}

const std::optional<Tenths> &Slide::relativeX() const noexcept
{
    return m_relativeX;
}

void Slide::setRelativeX(std::optional<Tenths> value)
{
    m_relativeX = std::move(value);
}

const std::optional<Tenths> &Slide::relativeY() const noexcept
{
    return m_relativeY;
}

void Slide::setRelativeY(std::optional<Tenths> value)
{
    m_relativeY = std::move(value);
}

const std::optional<FontFamily> &Slide::fontFamily() const noexcept
{
    return m_fontFamily;
}

void Slide::setFontFamily(std::optional<FontFamily> value)
{
    m_fontFamily = std::move(value);
}

const std::optional<FontStyle> &Slide::fontStyle() const noexcept
{
    return m_fontStyle;
}

void Slide::setFontStyle(std::optional<FontStyle> value)
{
    m_fontStyle = std::move(value);
}

const std::optional<FontSize> &Slide::fontSize() const noexcept
{
    return m_fontSize;
}

void Slide::setFontSize(std::optional<FontSize> value)
{
    m_fontSize = std::move(value);
}

const std::optional<FontWeight> &Slide::fontWeight() const noexcept
{
    return m_fontWeight;
}

void Slide::setFontWeight(std::optional<FontWeight> value)
{
    m_fontWeight = std::move(value);
}

const std::optional<Color> &Slide::color() const noexcept
{
    return m_color;
}

void Slide::setColor(std::optional<Color> value)
{
    m_color = std::move(value);
}

const std::optional<YesNo> &Slide::accelerate() const noexcept
{
    return m_accelerate;
}

void Slide::setAccelerate(std::optional<YesNo> value)
{
    m_accelerate = std::move(value);
}

const std::optional<TrillBeats> &Slide::beats() const noexcept
{
    return m_beats;
}

void Slide::setBeats(std::optional<TrillBeats> value)
{
    m_beats = std::move(value);
}

const std::optional<Percent> &Slide::firstBeat() const noexcept
{
    return m_firstBeat;
}

void Slide::setFirstBeat(std::optional<Percent> value)
{
    m_firstBeat = std::move(value);
}

const std::optional<Percent> &Slide::lastBeat() const noexcept
{
    return m_lastBeat;
}

void Slide::setLastBeat(std::optional<Percent> value)
{
    m_lastBeat = std::move(value);
}

const std::optional<Token> &Slide::id() const noexcept
{
    return m_id;
}

void Slide::setID(std::optional<Token> value)
{
    m_id = std::move(value);
}

const std::string &Slide::value() const noexcept
{
    return m_value;
}

void Slide::setValue(std::string value)
{
    m_value = std::move(value);
}

Slide parseSlide(pugi::xml_node el)
{
    Slide out;
    bool seen_type = false;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "type")
        {
            seen_type = true;
            out.setType(StartStop::parse(a.value()));
        }
        else if (aname == "number")
        {
            out.setNumber(NumberLevel::parse(a.value()));
        }
        else if (aname == "line-type")
        {
            out.setLineType(LineType::parse(a.value()));
        }
        else if (aname == "dash-length")
        {
            out.setDashLength(Tenths::parse(a.value()));
        }
        else if (aname == "space-length")
        {
            out.setSpaceLength(Tenths::parse(a.value()));
        }
        else if (aname == "default-x")
        {
            out.setDefaultX(Tenths::parse(a.value()));
        }
        else if (aname == "default-y")
        {
            out.setDefaultY(Tenths::parse(a.value()));
        }
        else if (aname == "relative-x")
        {
            out.setRelativeX(Tenths::parse(a.value()));
        }
        else if (aname == "relative-y")
        {
            out.setRelativeY(Tenths::parse(a.value()));
        }
        else if (aname == "font-family")
        {
            out.setFontFamily(FontFamily::parse(a.value()));
        }
        else if (aname == "font-style")
        {
            out.setFontStyle(FontStyle::parse(a.value()));
        }
        else if (aname == "font-size")
        {
            out.setFontSize(FontSize::parse(a.value()));
        }
        else if (aname == "font-weight")
        {
            out.setFontWeight(FontWeight::parse(a.value()));
        }
        else if (aname == "color")
        {
            out.setColor(Color::parse(a.value()));
        }
        else if (aname == "accelerate")
        {
            out.setAccelerate(YesNo::parse(a.value()));
        }
        else if (aname == "beats")
        {
            out.setBeats(TrillBeats::parse(a.value()));
        }
        else if (aname == "first-beat")
        {
            out.setFirstBeat(Percent::parse(a.value()));
        }
        else if (aname == "last-beat")
        {
            out.setLastBeat(Percent::parse(a.value()));
        }
        else if (aname == "id")
        {
            out.setID(Token::parse(a.value()));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    if (!seen_type)
    {
        throwMissingAttribute(el, "type");
    }
    parseSlideContent(out, el);
    return out;
}

void parseSlideContent(Slide &out, pugi::xml_node el)
{
    out.setValue(std::string{childText(el)});
    if (firstElement(el))
    {
        throwUnknownElement(firstElement(el));
    }
}

void serializeSlide(const Slide &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    setAttribute(el, "type", v.type().toString());
    if (v.number())
    {
        setAttribute(el, "number", v.number()->toString());
    }
    if (v.lineType())
    {
        setAttribute(el, "line-type", v.lineType()->toString());
    }
    if (v.dashLength())
    {
        setAttribute(el, "dash-length", v.dashLength()->toString());
    }
    if (v.spaceLength())
    {
        setAttribute(el, "space-length", v.spaceLength()->toString());
    }
    if (v.defaultX())
    {
        setAttribute(el, "default-x", v.defaultX()->toString());
    }
    if (v.defaultY())
    {
        setAttribute(el, "default-y", v.defaultY()->toString());
    }
    if (v.relativeX())
    {
        setAttribute(el, "relative-x", v.relativeX()->toString());
    }
    if (v.relativeY())
    {
        setAttribute(el, "relative-y", v.relativeY()->toString());
    }
    if (v.fontFamily())
    {
        setAttribute(el, "font-family", v.fontFamily()->toString());
    }
    if (v.fontStyle())
    {
        setAttribute(el, "font-style", v.fontStyle()->toString());
    }
    if (v.fontSize())
    {
        setAttribute(el, "font-size", v.fontSize()->toString());
    }
    if (v.fontWeight())
    {
        setAttribute(el, "font-weight", v.fontWeight()->toString());
    }
    if (v.color())
    {
        setAttribute(el, "color", v.color()->toString());
    }
    if (v.accelerate())
    {
        setAttribute(el, "accelerate", v.accelerate()->toString());
    }
    if (v.beats())
    {
        setAttribute(el, "beats", v.beats()->toString());
    }
    if (v.firstBeat())
    {
        setAttribute(el, "first-beat", v.firstBeat()->toString());
    }
    if (v.lastBeat())
    {
        setAttribute(el, "last-beat", v.lastBeat()->toString());
    }
    if (v.id())
    {
        setAttribute(el, "id", v.id()->toString());
    }
    serializeSlideContent(v, el);
}

void serializeSlideContent(const Slide &v, pugi::xml_node el)
{
    setText(el, v.value());
}

} // namespace mx::core
