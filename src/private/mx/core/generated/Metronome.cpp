// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/Metronome.h"

#include "mx/core/Lexical.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const std::optional<YesNo> &Metronome::parentheses() const noexcept
{
    return m_parentheses;
}

void Metronome::setParentheses(std::optional<YesNo> value)
{
    m_parentheses = std::move(value);
}

const std::optional<Tenths> &Metronome::defaultX() const noexcept
{
    return m_defaultX;
}

void Metronome::setDefaultX(std::optional<Tenths> value)
{
    m_defaultX = std::move(value);
}

const std::optional<Tenths> &Metronome::defaultY() const noexcept
{
    return m_defaultY;
}

void Metronome::setDefaultY(std::optional<Tenths> value)
{
    m_defaultY = std::move(value);
}

const std::optional<Tenths> &Metronome::relativeX() const noexcept
{
    return m_relativeX;
}

void Metronome::setRelativeX(std::optional<Tenths> value)
{
    m_relativeX = std::move(value);
}

const std::optional<Tenths> &Metronome::relativeY() const noexcept
{
    return m_relativeY;
}

void Metronome::setRelativeY(std::optional<Tenths> value)
{
    m_relativeY = std::move(value);
}

const std::optional<FontFamily> &Metronome::fontFamily() const noexcept
{
    return m_fontFamily;
}

void Metronome::setFontFamily(std::optional<FontFamily> value)
{
    m_fontFamily = std::move(value);
}

const std::optional<FontStyle> &Metronome::fontStyle() const noexcept
{
    return m_fontStyle;
}

void Metronome::setFontStyle(std::optional<FontStyle> value)
{
    m_fontStyle = std::move(value);
}

const std::optional<FontSize> &Metronome::fontSize() const noexcept
{
    return m_fontSize;
}

void Metronome::setFontSize(std::optional<FontSize> value)
{
    m_fontSize = std::move(value);
}

const std::optional<FontWeight> &Metronome::fontWeight() const noexcept
{
    return m_fontWeight;
}

void Metronome::setFontWeight(std::optional<FontWeight> value)
{
    m_fontWeight = std::move(value);
}

const std::optional<Color> &Metronome::color() const noexcept
{
    return m_color;
}

void Metronome::setColor(std::optional<Color> value)
{
    m_color = std::move(value);
}

const std::optional<LeftCenterRight> &Metronome::halign() const noexcept
{
    return m_halign;
}

void Metronome::setHalign(std::optional<LeftCenterRight> value)
{
    m_halign = std::move(value);
}

const std::optional<Valign> &Metronome::valign() const noexcept
{
    return m_valign;
}

void Metronome::setValign(std::optional<Valign> value)
{
    m_valign = std::move(value);
}

const std::optional<YesNo> &Metronome::printObject() const noexcept
{
    return m_printObject;
}

void Metronome::setPrintObject(std::optional<YesNo> value)
{
    m_printObject = std::move(value);
}

const std::optional<LeftCenterRight> &Metronome::justify() const noexcept
{
    return m_justify;
}

void Metronome::setJustify(std::optional<LeftCenterRight> value)
{
    m_justify = std::move(value);
}

const std::optional<Token> &Metronome::id() const noexcept
{
    return m_id;
}

void Metronome::setID(std::optional<Token> value)
{
    m_id = std::move(value);
}

const MetronomeChoice &Metronome::choice() const noexcept
{
    return m_choice;
}

void Metronome::setChoice(MetronomeChoice value)
{
    m_choice = std::move(value);
}

Metronome parseMetronome(pugi::xml_node el)
{
    Metronome out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "parentheses")
        {
            out.setParentheses(YesNo::parse(a.value()));
        }
        else if (aname == "default-x")
        {
            out.setDefaultX(Tenths::parse(a.value()));
        }
        else if (aname == "default-y")
        {
            out.setDefaultY(Tenths::parse(a.value()));
        }
        else if (aname == "relative-x")
        {
            out.setRelativeX(Tenths::parse(a.value()));
        }
        else if (aname == "relative-y")
        {
            out.setRelativeY(Tenths::parse(a.value()));
        }
        else if (aname == "font-family")
        {
            out.setFontFamily(FontFamily::parse(a.value()));
        }
        else if (aname == "font-style")
        {
            out.setFontStyle(FontStyle::parse(a.value()));
        }
        else if (aname == "font-size")
        {
            out.setFontSize(FontSize::parse(a.value()));
        }
        else if (aname == "font-weight")
        {
            out.setFontWeight(FontWeight::parse(a.value()));
        }
        else if (aname == "color")
        {
            out.setColor(Color::parse(a.value()));
        }
        else if (aname == "halign")
        {
            out.setHalign(LeftCenterRight::parse(a.value()));
        }
        else if (aname == "valign")
        {
            out.setValign(Valign::parse(a.value()));
        }
        else if (aname == "print-object")
        {
            out.setPrintObject(YesNo::parse(a.value()));
        }
        else if (aname == "justify")
        {
            out.setJustify(LeftCenterRight::parse(a.value()));
        }
        else if (aname == "id")
        {
            out.setID(Token::parse(a.value()));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    parseMetronomeContent(out, el);
    return out;
}

void parseMetronomeContent(Metronome &out, pugi::xml_node el)
{
    pugi::xml_node cursor = firstElement(el);
    if (cursor &&
        (cursorIs(cursor, "beat-unit") || cursorIs(cursor, "metronome-arrows") || cursorIs(cursor, "metronome-note")))
    {
        out.setChoice(parseMetronomeChoice(el, cursor));
    }
    else
    {
        throwMissingElement(el, "beat-unit");
    }
    if (cursor)
    {
        if (cursorIs(cursor, "beat-unit") || cursorIs(cursor, "beat-unit-dot") || cursorIs(cursor, "beat-unit-tied") ||
            cursorIs(cursor, "per-minute") || cursorIs(cursor, "metronome-arrows") ||
            cursorIs(cursor, "metronome-note") || cursorIs(cursor, "metronome-relation"))
        {
            throwMisplacedElement(cursor);
        }
        throwUnknownElement(cursor);
    }
}

void serializeMetronome(const Metronome &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    if (v.parentheses())
    {
        setAttribute(el, "parentheses", v.parentheses()->toString());
    }
    if (v.defaultX())
    {
        setAttribute(el, "default-x", v.defaultX()->toString());
    }
    if (v.defaultY())
    {
        setAttribute(el, "default-y", v.defaultY()->toString());
    }
    if (v.relativeX())
    {
        setAttribute(el, "relative-x", v.relativeX()->toString());
    }
    if (v.relativeY())
    {
        setAttribute(el, "relative-y", v.relativeY()->toString());
    }
    if (v.fontFamily())
    {
        setAttribute(el, "font-family", v.fontFamily()->toString());
    }
    if (v.fontStyle())
    {
        setAttribute(el, "font-style", v.fontStyle()->toString());
    }
    if (v.fontSize())
    {
        setAttribute(el, "font-size", v.fontSize()->toString());
    }
    if (v.fontWeight())
    {
        setAttribute(el, "font-weight", v.fontWeight()->toString());
    }
    if (v.color())
    {
        setAttribute(el, "color", v.color()->toString());
    }
    if (v.halign())
    {
        setAttribute(el, "halign", v.halign()->toString());
    }
    if (v.valign())
    {
        setAttribute(el, "valign", v.valign()->toString());
    }
    if (v.printObject())
    {
        setAttribute(el, "print-object", v.printObject()->toString());
    }
    if (v.justify())
    {
        setAttribute(el, "justify", v.justify()->toString());
    }
    if (v.id())
    {
        setAttribute(el, "id", v.id()->toString());
    }
    serializeMetronomeContent(v, el);
}

void serializeMetronomeContent(const Metronome &v, pugi::xml_node el)
{
    serializeMetronomeChoice(v.choice(), el);
}

} // namespace mx::core
