// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/SmuflSegnoGlyphName.h"

#include <cstddef>
#include <iterator>
#include <utility>

namespace mx::core
{

namespace
{

constexpr std::string_view kPrefix[] = {
    "segno",
};

// The ASCII subset of XML name characters the schema's \c denotes.
bool isNameChar(char c) noexcept
{
    return (c >= 'a' && c <= 'z') || (c >= 'A' && c <= 'Z') || (c >= '0' && c <= '9') || c == '-' || c == '.' ||
           c == ':' || c == '_';
}

} // namespace

SmuflSegnoGlyphName::SmuflSegnoGlyphName()
{
    repair();
}

SmuflSegnoGlyphName::SmuflSegnoGlyphName(std::string suffix) : m_suffix{std::move(suffix)}
{
    repair();
}

void SmuflSegnoGlyphName::setSuffix(std::string value)
{
    m_suffix = std::move(value);
    repair();
}

void SmuflSegnoGlyphName::repair()
{
    std::string cleaned;
    cleaned.reserve(m_suffix.size());
    for (const char c : m_suffix)
    {
        if (isNameChar(c))
        {
            cleaned.push_back(c);
        }
    }
    m_suffix = std::move(cleaned);
}

std::string SmuflSegnoGlyphName::toString() const
{
    std::string out{kPrefix[0]};
    out += m_suffix;
    return out;
}

bool SmuflSegnoGlyphName::tryParse(std::string_view text, SmuflSegnoGlyphName &out)
{
    for (std::size_t i = 0; i < std::size(kPrefix); ++i)
    {
        const std::string_view prefix = kPrefix[i];
        if (text.size() < prefix.size() || text.substr(0, prefix.size()) != prefix)
        {
            continue;
        }
        const std::string_view suffix = text.substr(prefix.size());
        bool valid = true;
        for (const char c : suffix)
        {
            if (!isNameChar(c))
            {
                valid = false;
                break;
            }
        }
        if (!valid)
        {
            continue;
        }
        out = SmuflSegnoGlyphName{std::string{suffix}};
        return true;
    }
    return false;
}

SmuflSegnoGlyphName SmuflSegnoGlyphName::parse(std::string_view text)
{
    SmuflSegnoGlyphName out;
    if (tryParse(text, out))
    {
        return out;
    }
    // Repair: keep what we can of the text as a suffix.
    return SmuflSegnoGlyphName{std::string{text}};
}

} // namespace mx::core
