// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/Tuplet.h"

#include "mx/core/Lexical.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const StartStop &Tuplet::type() const noexcept
{
    return m_type;
}

void Tuplet::setType(StartStop value)
{
    m_type = std::move(value);
}

const std::optional<NumberLevel> &Tuplet::number() const noexcept
{
    return m_number;
}

void Tuplet::setNumber(std::optional<NumberLevel> value)
{
    m_number = std::move(value);
}

const std::optional<YesNo> &Tuplet::bracket() const noexcept
{
    return m_bracket;
}

void Tuplet::setBracket(std::optional<YesNo> value)
{
    m_bracket = std::move(value);
}

const std::optional<ShowTuplet> &Tuplet::showNumber() const noexcept
{
    return m_showNumber;
}

void Tuplet::setShowNumber(std::optional<ShowTuplet> value)
{
    m_showNumber = std::move(value);
}

const std::optional<ShowTuplet> &Tuplet::showType() const noexcept
{
    return m_showType;
}

void Tuplet::setShowType(std::optional<ShowTuplet> value)
{
    m_showType = std::move(value);
}

const std::optional<LineShape> &Tuplet::lineShape() const noexcept
{
    return m_lineShape;
}

void Tuplet::setLineShape(std::optional<LineShape> value)
{
    m_lineShape = std::move(value);
}

const std::optional<Tenths> &Tuplet::defaultX() const noexcept
{
    return m_defaultX;
}

void Tuplet::setDefaultX(std::optional<Tenths> value)
{
    m_defaultX = std::move(value);
}

const std::optional<Tenths> &Tuplet::defaultY() const noexcept
{
    return m_defaultY;
}

void Tuplet::setDefaultY(std::optional<Tenths> value)
{
    m_defaultY = std::move(value);
}

const std::optional<Tenths> &Tuplet::relativeX() const noexcept
{
    return m_relativeX;
}

void Tuplet::setRelativeX(std::optional<Tenths> value)
{
    m_relativeX = std::move(value);
}

const std::optional<Tenths> &Tuplet::relativeY() const noexcept
{
    return m_relativeY;
}

void Tuplet::setRelativeY(std::optional<Tenths> value)
{
    m_relativeY = std::move(value);
}

const std::optional<AboveBelow> &Tuplet::placement() const noexcept
{
    return m_placement;
}

void Tuplet::setPlacement(std::optional<AboveBelow> value)
{
    m_placement = std::move(value);
}

const std::optional<Token> &Tuplet::id() const noexcept
{
    return m_id;
}

void Tuplet::setID(std::optional<Token> value)
{
    m_id = std::move(value);
}

const std::optional<TupletPortion> &Tuplet::tupletActual() const noexcept
{
    return m_tupletActual;
}

void Tuplet::setTupletActual(std::optional<TupletPortion> value)
{
    m_tupletActual = std::move(value);
}

const std::optional<TupletPortion> &Tuplet::tupletNormal() const noexcept
{
    return m_tupletNormal;
}

void Tuplet::setTupletNormal(std::optional<TupletPortion> value)
{
    m_tupletNormal = std::move(value);
}

Tuplet parseTuplet(pugi::xml_node el)
{
    Tuplet out;
    bool seen_type = false;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "type")
        {
            seen_type = true;
            out.setType(StartStop::parse(a.value()));
        }
        else if (aname == "number")
        {
            out.setNumber(NumberLevel::parse(a.value()));
        }
        else if (aname == "bracket")
        {
            out.setBracket(YesNo::parse(a.value()));
        }
        else if (aname == "show-number")
        {
            out.setShowNumber(ShowTuplet::parse(a.value()));
        }
        else if (aname == "show-type")
        {
            out.setShowType(ShowTuplet::parse(a.value()));
        }
        else if (aname == "line-shape")
        {
            out.setLineShape(LineShape::parse(a.value()));
        }
        else if (aname == "default-x")
        {
            out.setDefaultX(Tenths::parse(a.value()));
        }
        else if (aname == "default-y")
        {
            out.setDefaultY(Tenths::parse(a.value()));
        }
        else if (aname == "relative-x")
        {
            out.setRelativeX(Tenths::parse(a.value()));
        }
        else if (aname == "relative-y")
        {
            out.setRelativeY(Tenths::parse(a.value()));
        }
        else if (aname == "placement")
        {
            out.setPlacement(AboveBelow::parse(a.value()));
        }
        else if (aname == "id")
        {
            out.setID(Token::parse(a.value()));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    if (!seen_type)
    {
        throwMissingAttribute(el, "type");
    }
    parseTupletContent(out, el);
    return out;
}

void parseTupletContent(Tuplet &out, pugi::xml_node el)
{
    pugi::xml_node cursor = firstElement(el);
    if (cursorIs(cursor, "tuplet-actual"))
    {
        out.setTupletActual(parseTupletPortion(cursor));
        cursor = nextElement(cursor);
    }
    if (cursorIs(cursor, "tuplet-normal"))
    {
        out.setTupletNormal(parseTupletPortion(cursor));
        cursor = nextElement(cursor);
    }
    if (cursor)
    {
        if (cursorIs(cursor, "tuplet-actual") || cursorIs(cursor, "tuplet-normal"))
        {
            throwMisplacedElement(cursor);
        }
        throwUnknownElement(cursor);
    }
}

void serializeTuplet(const Tuplet &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    setAttribute(el, "type", v.type().toString());
    if (v.number())
    {
        setAttribute(el, "number", v.number()->toString());
    }
    if (v.bracket())
    {
        setAttribute(el, "bracket", v.bracket()->toString());
    }
    if (v.showNumber())
    {
        setAttribute(el, "show-number", v.showNumber()->toString());
    }
    if (v.showType())
    {
        setAttribute(el, "show-type", v.showType()->toString());
    }
    if (v.lineShape())
    {
        setAttribute(el, "line-shape", v.lineShape()->toString());
    }
    if (v.defaultX())
    {
        setAttribute(el, "default-x", v.defaultX()->toString());
    }
    if (v.defaultY())
    {
        setAttribute(el, "default-y", v.defaultY()->toString());
    }
    if (v.relativeX())
    {
        setAttribute(el, "relative-x", v.relativeX()->toString());
    }
    if (v.relativeY())
    {
        setAttribute(el, "relative-y", v.relativeY()->toString());
    }
    if (v.placement())
    {
        setAttribute(el, "placement", v.placement()->toString());
    }
    if (v.id())
    {
        setAttribute(el, "id", v.id()->toString());
    }
    serializeTupletContent(v, el);
}

void serializeTupletContent(const Tuplet &v, pugi::xml_node el)
{
    if (v.tupletActual())
    {
        serializeTupletPortion(*v.tupletActual(), el, "tuplet-actual");
    }
    if (v.tupletNormal())
    {
        serializeTupletPortion(*v.tupletNormal(), el, "tuplet-normal");
    }
}

} // namespace mx::core
