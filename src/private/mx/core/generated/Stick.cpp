// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/Stick.h"

#include "mx/core/Lexical.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const std::optional<TipDirection> &Stick::tip() const noexcept
{
    return m_tip;
}

void Stick::setTip(std::optional<TipDirection> value)
{
    m_tip = std::move(value);
}

const std::optional<YesNo> &Stick::parentheses() const noexcept
{
    return m_parentheses;
}

void Stick::setParentheses(std::optional<YesNo> value)
{
    m_parentheses = std::move(value);
}

const std::optional<YesNo> &Stick::dashedCircle() const noexcept
{
    return m_dashedCircle;
}

void Stick::setDashedCircle(std::optional<YesNo> value)
{
    m_dashedCircle = std::move(value);
}

const StickType &Stick::stickType() const noexcept
{
    return m_stickType;
}

void Stick::setStickType(StickType value)
{
    m_stickType = std::move(value);
}

const StickMaterial &Stick::stickMaterial() const noexcept
{
    return m_stickMaterial;
}

void Stick::setStickMaterial(StickMaterial value)
{
    m_stickMaterial = std::move(value);
}

Stick parseStick(pugi::xml_node el)
{
    Stick out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "tip")
        {
            out.setTip(TipDirection::parse(a.value()));
        }
        else if (aname == "parentheses")
        {
            out.setParentheses(YesNo::parse(a.value()));
        }
        else if (aname == "dashed-circle")
        {
            out.setDashedCircle(YesNo::parse(a.value()));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    parseStickContent(out, el);
    return out;
}

void parseStickContent(Stick &out, pugi::xml_node el)
{
    pugi::xml_node cursor = firstElement(el);
    if (cursorIs(cursor, "stick-type"))
    {
        out.setStickType(StickType::parse(childText(cursor)));
        cursor = nextElement(cursor);
    }
    else
    {
        throwMissingOrMisplaced(el, cursor, "stick-type");
    }
    if (cursorIs(cursor, "stick-material"))
    {
        out.setStickMaterial(StickMaterial::parse(childText(cursor)));
        cursor = nextElement(cursor);
    }
    else
    {
        throwMissingOrMisplaced(el, cursor, "stick-material");
    }
    if (cursor)
    {
        if (cursorIs(cursor, "stick-type") || cursorIs(cursor, "stick-material"))
        {
            throwMisplacedElement(cursor);
        }
        throwUnknownElement(cursor);
    }
}

void serializeStick(const Stick &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    if (v.tip())
    {
        setAttribute(el, "tip", v.tip()->toString());
    }
    if (v.parentheses())
    {
        setAttribute(el, "parentheses", v.parentheses()->toString());
    }
    if (v.dashedCircle())
    {
        setAttribute(el, "dashed-circle", v.dashedCircle()->toString());
    }
    serializeStickContent(v, el);
}

void serializeStickContent(const Stick &v, pugi::xml_node el)
{
    writeTextElement(el, "stick-type", v.stickType().toString());
    writeTextElement(el, "stick-material", v.stickMaterial().toString());
}

} // namespace mx::core
