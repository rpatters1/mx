// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/Assess.h"

#include "mx/core/Lexical.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const YesNo &Assess::type() const noexcept
{
    return m_type;
}

void Assess::setType(YesNo value)
{
    m_type = std::move(value);
}

const std::optional<Token> &Assess::player() const noexcept
{
    return m_player;
}

void Assess::setPlayer(std::optional<Token> value)
{
    m_player = std::move(value);
}

const std::optional<TimeOnly> &Assess::timeOnly() const noexcept
{
    return m_timeOnly;
}

void Assess::setTimeOnly(std::optional<TimeOnly> value)
{
    m_timeOnly = std::move(value);
}

Assess parseAssess(pugi::xml_node el)
{
    Assess out;
    bool seen_type = false;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "type")
        {
            seen_type = true;
            out.setType(YesNo::parse(a.value()));
        }
        else if (aname == "player")
        {
            out.setPlayer(Token::parse(a.value()));
        }
        else if (aname == "time-only")
        {
            out.setTimeOnly(TimeOnly::parse(a.value()));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    if (!seen_type)
    {
        throwMissingAttribute(el, "type");
    }
    parseAssessContent(out, el);
    return out;
}

void parseAssessContent(Assess &out, pugi::xml_node el)
{
    (void)out;
    if (firstElement(el))
    {
        throwUnknownElement(firstElement(el));
    }
}

void serializeAssess(const Assess &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    setAttribute(el, "type", v.type().toString());
    if (v.player())
    {
        setAttribute(el, "player", v.player()->toString());
    }
    if (v.timeOnly())
    {
        setAttribute(el, "time-only", v.timeOnly()->toString());
    }
    serializeAssessContent(v, el);
}

void serializeAssessContent(const Assess &v, pugi::xml_node el)
{
    (void)v;
    (void)el;
}

} // namespace mx::core
