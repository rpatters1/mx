// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/TuningGroup.h"

#include "mx/core/Lexical.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const Step &TuningGroup::tuningStep() const noexcept
{
    return m_tuningStep;
}

void TuningGroup::setTuningStep(Step value)
{
    m_tuningStep = std::move(value);
}

const std::optional<Semitones> &TuningGroup::tuningAlter() const noexcept
{
    return m_tuningAlter;
}

void TuningGroup::setTuningAlter(std::optional<Semitones> value)
{
    m_tuningAlter = std::move(value);
}

const Octave &TuningGroup::tuningOctave() const noexcept
{
    return m_tuningOctave;
}

void TuningGroup::setTuningOctave(Octave value)
{
    m_tuningOctave = std::move(value);
}

TuningGroup parseTuningGroup(pugi::xml_node el, pugi::xml_node &cursor)
{
    TuningGroup out;
    if (cursorIs(cursor, "tuning-step"))
    {
        out.setTuningStep(Step::parse(childText(cursor)));
        cursor = nextElement(cursor);
    }
    else
    {
        throwMissingOrMisplaced(el, cursor, "tuning-step");
    }
    if (cursorIs(cursor, "tuning-alter"))
    {
        out.setTuningAlter(Semitones::parse(childText(cursor)));
        cursor = nextElement(cursor);
    }
    if (cursorIs(cursor, "tuning-octave"))
    {
        out.setTuningOctave(Octave::parse(childText(cursor)));
        cursor = nextElement(cursor);
    }
    else
    {
        throwMissingOrMisplaced(el, cursor, "tuning-octave");
    }
    return out;
}

void serializeTuningGroup(const TuningGroup &v, pugi::xml_node el)
{
    writeTextElement(el, "tuning-step", v.tuningStep().toString());
    if (v.tuningAlter())
    {
        writeTextElement(el, "tuning-alter", v.tuningAlter()->toString());
    }
    writeTextElement(el, "tuning-octave", v.tuningOctave().toString());
}

} // namespace mx::core
