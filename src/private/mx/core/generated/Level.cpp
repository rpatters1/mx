// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/Level.h"

#include "mx/core/Lexical.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const std::optional<YesNo> &Level::reference() const noexcept
{
    return m_reference;
}

void Level::setReference(std::optional<YesNo> value)
{
    m_reference = std::move(value);
}

const std::optional<StartStopSingle> &Level::type() const noexcept
{
    return m_type;
}

void Level::setType(std::optional<StartStopSingle> value)
{
    m_type = std::move(value);
}

const std::optional<YesNo> &Level::parentheses() const noexcept
{
    return m_parentheses;
}

void Level::setParentheses(std::optional<YesNo> value)
{
    m_parentheses = std::move(value);
}

const std::optional<YesNo> &Level::bracket() const noexcept
{
    return m_bracket;
}

void Level::setBracket(std::optional<YesNo> value)
{
    m_bracket = std::move(value);
}

const std::optional<SymbolSize> &Level::size() const noexcept
{
    return m_size;
}

void Level::setSize(std::optional<SymbolSize> value)
{
    m_size = std::move(value);
}

const std::string &Level::value() const noexcept
{
    return m_value;
}

void Level::setValue(std::string value)
{
    m_value = std::move(value);
}

Level parseLevel(pugi::xml_node el)
{
    Level out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "reference")
        {
            out.setReference(YesNo::parse(a.value()));
        }
        else if (aname == "type")
        {
            out.setType(StartStopSingle::parse(a.value()));
        }
        else if (aname == "parentheses")
        {
            out.setParentheses(YesNo::parse(a.value()));
        }
        else if (aname == "bracket")
        {
            out.setBracket(YesNo::parse(a.value()));
        }
        else if (aname == "size")
        {
            out.setSize(SymbolSize::parse(a.value()));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    parseLevelContent(out, el);
    return out;
}

void parseLevelContent(Level &out, pugi::xml_node el)
{
    out.setValue(std::string{childText(el)});
    if (firstElement(el))
    {
        throwUnknownElement(firstElement(el));
    }
}

void serializeLevel(const Level &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    if (v.reference())
    {
        setAttribute(el, "reference", v.reference()->toString());
    }
    if (v.type())
    {
        setAttribute(el, "type", v.type()->toString());
    }
    if (v.parentheses())
    {
        setAttribute(el, "parentheses", v.parentheses()->toString());
    }
    if (v.bracket())
    {
        setAttribute(el, "bracket", v.bracket()->toString());
    }
    if (v.size())
    {
        setAttribute(el, "size", v.size()->toString());
    }
    serializeLevelContent(v, el);
}

void serializeLevelContent(const Level &v, pugi::xml_node el)
{
    setText(el, v.value());
}

} // namespace mx::core
