// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/NoteSize.h"

#include "mx/core/Lexical.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const NoteSizeType &NoteSize::type() const noexcept
{
    return m_type;
}

void NoteSize::setType(NoteSizeType value)
{
    m_type = std::move(value);
}

const NonNegativeDecimal &NoteSize::value() const noexcept
{
    return m_value;
}

void NoteSize::setValue(NonNegativeDecimal value)
{
    m_value = std::move(value);
}

NoteSize parseNoteSize(pugi::xml_node el)
{
    NoteSize out;
    bool seen_type = false;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "type")
        {
            seen_type = true;
            out.setType(NoteSizeType::parse(a.value()));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    if (!seen_type)
    {
        throwMissingAttribute(el, "type");
    }
    parseNoteSizeContent(out, el);
    return out;
}

void parseNoteSizeContent(NoteSize &out, pugi::xml_node el)
{
    out.setValue(NonNegativeDecimal::parse(childText(el)));
    if (firstElement(el))
    {
        throwUnknownElement(firstElement(el));
    }
}

void serializeNoteSize(const NoteSize &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    setAttribute(el, "type", v.type().toString());
    serializeNoteSizeContent(v, el);
}

void serializeNoteSizeContent(const NoteSize &v, pugi::xml_node el)
{
    setText(el, v.value().toString());
}

} // namespace mx::core
