// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/StaffLayout.h"

#include "mx/core/Lexical.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const std::optional<StaffNumber> &StaffLayout::number() const noexcept
{
    return m_number;
}

void StaffLayout::setNumber(std::optional<StaffNumber> value)
{
    m_number = std::move(value);
}

const std::optional<Tenths> &StaffLayout::staffDistance() const noexcept
{
    return m_staffDistance;
}

void StaffLayout::setStaffDistance(std::optional<Tenths> value)
{
    m_staffDistance = std::move(value);
}

StaffLayout parseStaffLayout(pugi::xml_node el)
{
    StaffLayout out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "number")
        {
            out.setNumber(StaffNumber::parse(a.value()));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    parseStaffLayoutContent(out, el);
    return out;
}

void parseStaffLayoutContent(StaffLayout &out, pugi::xml_node el)
{
    pugi::xml_node cursor = firstElement(el);
    if (cursorIs(cursor, "staff-distance"))
    {
        out.setStaffDistance(Tenths::parse(childText(cursor)));
        cursor = nextElement(cursor);
    }
    if (cursor)
    {
        if (cursorIs(cursor, "staff-distance"))
        {
            throwMisplacedElement(cursor);
        }
        throwUnknownElement(cursor);
    }
}

void serializeStaffLayout(const StaffLayout &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    if (v.number())
    {
        setAttribute(el, "number", v.number()->toString());
    }
    serializeStaffLayoutContent(v, el);
}

void serializeStaffLayoutContent(const StaffLayout &v, pugi::xml_node el)
{
    if (v.staffDistance())
    {
        writeTextElement(el, "staff-distance", v.staffDistance()->toString());
    }
}

} // namespace mx::core
