// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/MeasureRepeat.h"

#include "mx/core/Lexical.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const StartStop &MeasureRepeat::type() const noexcept
{
    return m_type;
}

void MeasureRepeat::setType(StartStop value)
{
    m_type = std::move(value);
}

const std::optional<int> &MeasureRepeat::slashes() const noexcept
{
    return m_slashes;
}

void MeasureRepeat::setSlashes(std::optional<int> value)
{
    m_slashes = std::move(value);
}

const PositiveIntegerOrEmpty &MeasureRepeat::value() const noexcept
{
    return m_value;
}

void MeasureRepeat::setValue(PositiveIntegerOrEmpty value)
{
    m_value = std::move(value);
}

MeasureRepeat parseMeasureRepeat(pugi::xml_node el)
{
    MeasureRepeat out;
    bool seen_type = false;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "type")
        {
            seen_type = true;
            out.setType(StartStop::parse(a.value()));
        }
        else if (aname == "slashes")
        {
            out.setSlashes(parseInt(a.value()));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    if (!seen_type)
    {
        throwMissingAttribute(el, "type");
    }
    parseMeasureRepeatContent(out, el);
    return out;
}

void parseMeasureRepeatContent(MeasureRepeat &out, pugi::xml_node el)
{
    out.setValue(PositiveIntegerOrEmpty::parse(childText(el)));
    if (firstElement(el))
    {
        throwUnknownElement(firstElement(el));
    }
}

void serializeMeasureRepeat(const MeasureRepeat &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    setAttribute(el, "type", v.type().toString());
    if (v.slashes())
    {
        setAttribute(el, "slashes", formatInt(*v.slashes()));
    }
    serializeMeasureRepeatContent(v, el);
}

void serializeMeasureRepeatContent(const MeasureRepeat &v, pugi::xml_node el)
{
    setText(el, v.value().toString());
}

} // namespace mx::core
