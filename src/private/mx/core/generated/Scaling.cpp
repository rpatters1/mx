// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/Scaling.h"

#include "mx/core/Lexical.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const Millimeters &Scaling::millimeters() const noexcept
{
    return m_millimeters;
}

void Scaling::setMillimeters(Millimeters value)
{
    m_millimeters = std::move(value);
}

const Tenths &Scaling::tenths() const noexcept
{
    return m_tenths;
}

void Scaling::setTenths(Tenths value)
{
    m_tenths = std::move(value);
}

Scaling parseScaling(pugi::xml_node el)
{
    Scaling out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        throwUnknownAttribute(el, a.name());
    }
    parseScalingContent(out, el);
    return out;
}

void parseScalingContent(Scaling &out, pugi::xml_node el)
{
    pugi::xml_node cursor = firstElement(el);
    if (cursorIs(cursor, "millimeters"))
    {
        out.setMillimeters(Millimeters::parse(childText(cursor)));
        cursor = nextElement(cursor);
    }
    else
    {
        throwMissingOrMisplaced(el, cursor, "millimeters");
    }
    if (cursorIs(cursor, "tenths"))
    {
        out.setTenths(Tenths::parse(childText(cursor)));
        cursor = nextElement(cursor);
    }
    else
    {
        throwMissingOrMisplaced(el, cursor, "tenths");
    }
    if (cursor)
    {
        if (cursorIs(cursor, "millimeters") || cursorIs(cursor, "tenths"))
        {
            throwMisplacedElement(cursor);
        }
        throwUnknownElement(cursor);
    }
}

void serializeScaling(const Scaling &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    serializeScalingContent(v, el);
}

void serializeScalingContent(const Scaling &v, pugi::xml_node el)
{
    writeTextElement(el, "millimeters", v.millimeters().toString());
    writeTextElement(el, "tenths", v.tenths().toString());
}

} // namespace mx::core
