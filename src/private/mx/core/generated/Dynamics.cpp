// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/Dynamics.h"

#include "mx/core/Lexical.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const std::optional<Tenths> &Dynamics::defaultX() const noexcept
{
    return m_defaultX;
}

void Dynamics::setDefaultX(std::optional<Tenths> value)
{
    m_defaultX = std::move(value);
}

const std::optional<Tenths> &Dynamics::defaultY() const noexcept
{
    return m_defaultY;
}

void Dynamics::setDefaultY(std::optional<Tenths> value)
{
    m_defaultY = std::move(value);
}

const std::optional<Tenths> &Dynamics::relativeX() const noexcept
{
    return m_relativeX;
}

void Dynamics::setRelativeX(std::optional<Tenths> value)
{
    m_relativeX = std::move(value);
}

const std::optional<Tenths> &Dynamics::relativeY() const noexcept
{
    return m_relativeY;
}

void Dynamics::setRelativeY(std::optional<Tenths> value)
{
    m_relativeY = std::move(value);
}

const std::optional<FontFamily> &Dynamics::fontFamily() const noexcept
{
    return m_fontFamily;
}

void Dynamics::setFontFamily(std::optional<FontFamily> value)
{
    m_fontFamily = std::move(value);
}

const std::optional<FontStyle> &Dynamics::fontStyle() const noexcept
{
    return m_fontStyle;
}

void Dynamics::setFontStyle(std::optional<FontStyle> value)
{
    m_fontStyle = std::move(value);
}

const std::optional<FontSize> &Dynamics::fontSize() const noexcept
{
    return m_fontSize;
}

void Dynamics::setFontSize(std::optional<FontSize> value)
{
    m_fontSize = std::move(value);
}

const std::optional<FontWeight> &Dynamics::fontWeight() const noexcept
{
    return m_fontWeight;
}

void Dynamics::setFontWeight(std::optional<FontWeight> value)
{
    m_fontWeight = std::move(value);
}

const std::optional<Color> &Dynamics::color() const noexcept
{
    return m_color;
}

void Dynamics::setColor(std::optional<Color> value)
{
    m_color = std::move(value);
}

const std::optional<LeftCenterRight> &Dynamics::halign() const noexcept
{
    return m_halign;
}

void Dynamics::setHalign(std::optional<LeftCenterRight> value)
{
    m_halign = std::move(value);
}

const std::optional<Valign> &Dynamics::valign() const noexcept
{
    return m_valign;
}

void Dynamics::setValign(std::optional<Valign> value)
{
    m_valign = std::move(value);
}

const std::optional<AboveBelow> &Dynamics::placement() const noexcept
{
    return m_placement;
}

void Dynamics::setPlacement(std::optional<AboveBelow> value)
{
    m_placement = std::move(value);
}

const std::optional<NumberOfLines> &Dynamics::underline() const noexcept
{
    return m_underline;
}

void Dynamics::setUnderline(std::optional<NumberOfLines> value)
{
    m_underline = std::move(value);
}

const std::optional<NumberOfLines> &Dynamics::overline() const noexcept
{
    return m_overline;
}

void Dynamics::setOverline(std::optional<NumberOfLines> value)
{
    m_overline = std::move(value);
}

const std::optional<NumberOfLines> &Dynamics::lineThrough() const noexcept
{
    return m_lineThrough;
}

void Dynamics::setLineThrough(std::optional<NumberOfLines> value)
{
    m_lineThrough = std::move(value);
}

const std::optional<EnclosureShape> &Dynamics::enclosure() const noexcept
{
    return m_enclosure;
}

void Dynamics::setEnclosure(std::optional<EnclosureShape> value)
{
    m_enclosure = std::move(value);
}

const std::optional<Token> &Dynamics::id() const noexcept
{
    return m_id;
}

void Dynamics::setID(std::optional<Token> value)
{
    m_id = std::move(value);
}

std::span<const DynamicsChoice> Dynamics::choice() const noexcept
{
    return m_choice;
}

void Dynamics::addChoice(DynamicsChoice value)
{
    m_choice.push_back(std::move(value));
}

void Dynamics::setChoice(std::vector<DynamicsChoice> value)
{
    m_choice = std::move(value);
}

Dynamics parseDynamics(pugi::xml_node el)
{
    Dynamics out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "default-x")
        {
            out.setDefaultX(Tenths::parse(a.value()));
        }
        else if (aname == "default-y")
        {
            out.setDefaultY(Tenths::parse(a.value()));
        }
        else if (aname == "relative-x")
        {
            out.setRelativeX(Tenths::parse(a.value()));
        }
        else if (aname == "relative-y")
        {
            out.setRelativeY(Tenths::parse(a.value()));
        }
        else if (aname == "font-family")
        {
            out.setFontFamily(FontFamily::parse(a.value()));
        }
        else if (aname == "font-style")
        {
            out.setFontStyle(FontStyle::parse(a.value()));
        }
        else if (aname == "font-size")
        {
            out.setFontSize(FontSize::parse(a.value()));
        }
        else if (aname == "font-weight")
        {
            out.setFontWeight(FontWeight::parse(a.value()));
        }
        else if (aname == "color")
        {
            out.setColor(Color::parse(a.value()));
        }
        else if (aname == "halign")
        {
            out.setHalign(LeftCenterRight::parse(a.value()));
        }
        else if (aname == "valign")
        {
            out.setValign(Valign::parse(a.value()));
        }
        else if (aname == "placement")
        {
            out.setPlacement(AboveBelow::parse(a.value()));
        }
        else if (aname == "underline")
        {
            out.setUnderline(NumberOfLines::parse(a.value()));
        }
        else if (aname == "overline")
        {
            out.setOverline(NumberOfLines::parse(a.value()));
        }
        else if (aname == "line-through")
        {
            out.setLineThrough(NumberOfLines::parse(a.value()));
        }
        else if (aname == "enclosure")
        {
            out.setEnclosure(EnclosureShape::parse(a.value()));
        }
        else if (aname == "id")
        {
            out.setID(Token::parse(a.value()));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    parseDynamicsContent(out, el);
    return out;
}

void parseDynamicsContent(Dynamics &out, pugi::xml_node el)
{
    pugi::xml_node cursor = firstElement(el);
    while (cursor &&
           (cursorIs(cursor, "p") || cursorIs(cursor, "pp") || cursorIs(cursor, "ppp") || cursorIs(cursor, "pppp") ||
            cursorIs(cursor, "ppppp") || cursorIs(cursor, "pppppp") || cursorIs(cursor, "f") ||
            cursorIs(cursor, "ff") || cursorIs(cursor, "fff") || cursorIs(cursor, "ffff") ||
            cursorIs(cursor, "fffff") || cursorIs(cursor, "ffffff") || cursorIs(cursor, "mp") ||
            cursorIs(cursor, "mf") || cursorIs(cursor, "sf") || cursorIs(cursor, "sfp") || cursorIs(cursor, "sfpp") ||
            cursorIs(cursor, "fp") || cursorIs(cursor, "rf") || cursorIs(cursor, "rfz") || cursorIs(cursor, "sfz") ||
            cursorIs(cursor, "sffz") || cursorIs(cursor, "fz") || cursorIs(cursor, "n") || cursorIs(cursor, "pf") ||
            cursorIs(cursor, "sfzp") || cursorIs(cursor, "other-dynamics")))
    {
        out.addChoice(parseDynamicsChoice(el, cursor));
    }
    if (cursor)
    {
        if (cursorIs(cursor, "p") || cursorIs(cursor, "pp") || cursorIs(cursor, "ppp") || cursorIs(cursor, "pppp") ||
            cursorIs(cursor, "ppppp") || cursorIs(cursor, "pppppp") || cursorIs(cursor, "f") ||
            cursorIs(cursor, "ff") || cursorIs(cursor, "fff") || cursorIs(cursor, "ffff") ||
            cursorIs(cursor, "fffff") || cursorIs(cursor, "ffffff") || cursorIs(cursor, "mp") ||
            cursorIs(cursor, "mf") || cursorIs(cursor, "sf") || cursorIs(cursor, "sfp") || cursorIs(cursor, "sfpp") ||
            cursorIs(cursor, "fp") || cursorIs(cursor, "rf") || cursorIs(cursor, "rfz") || cursorIs(cursor, "sfz") ||
            cursorIs(cursor, "sffz") || cursorIs(cursor, "fz") || cursorIs(cursor, "n") || cursorIs(cursor, "pf") ||
            cursorIs(cursor, "sfzp") || cursorIs(cursor, "other-dynamics"))
        {
            throwMisplacedElement(cursor);
        }
        throwUnknownElement(cursor);
    }
}

void serializeDynamics(const Dynamics &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    if (v.defaultX())
    {
        setAttribute(el, "default-x", v.defaultX()->toString());
    }
    if (v.defaultY())
    {
        setAttribute(el, "default-y", v.defaultY()->toString());
    }
    if (v.relativeX())
    {
        setAttribute(el, "relative-x", v.relativeX()->toString());
    }
    if (v.relativeY())
    {
        setAttribute(el, "relative-y", v.relativeY()->toString());
    }
    if (v.fontFamily())
    {
        setAttribute(el, "font-family", v.fontFamily()->toString());
    }
    if (v.fontStyle())
    {
        setAttribute(el, "font-style", v.fontStyle()->toString());
    }
    if (v.fontSize())
    {
        setAttribute(el, "font-size", v.fontSize()->toString());
    }
    if (v.fontWeight())
    {
        setAttribute(el, "font-weight", v.fontWeight()->toString());
    }
    if (v.color())
    {
        setAttribute(el, "color", v.color()->toString());
    }
    if (v.halign())
    {
        setAttribute(el, "halign", v.halign()->toString());
    }
    if (v.valign())
    {
        setAttribute(el, "valign", v.valign()->toString());
    }
    if (v.placement())
    {
        setAttribute(el, "placement", v.placement()->toString());
    }
    if (v.underline())
    {
        setAttribute(el, "underline", v.underline()->toString());
    }
    if (v.overline())
    {
        setAttribute(el, "overline", v.overline()->toString());
    }
    if (v.lineThrough())
    {
        setAttribute(el, "line-through", v.lineThrough()->toString());
    }
    if (v.enclosure())
    {
        setAttribute(el, "enclosure", v.enclosure()->toString());
    }
    if (v.id())
    {
        setAttribute(el, "id", v.id()->toString());
    }
    serializeDynamicsContent(v, el);
}

void serializeDynamicsContent(const Dynamics &v, pugi::xml_node el)
{
    for (const auto &item : v.choice())
    {
        serializeDynamicsChoice(item, el);
    }
}

} // namespace mx::core
