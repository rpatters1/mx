// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/Sync.h"

#include "mx/core/Lexical.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const SyncType &Sync::type() const noexcept
{
    return m_type;
}

void Sync::setType(SyncType value)
{
    m_type = std::move(value);
}

const std::optional<Milliseconds> &Sync::latency() const noexcept
{
    return m_latency;
}

void Sync::setLatency(std::optional<Milliseconds> value)
{
    m_latency = std::move(value);
}

const std::optional<Token> &Sync::player() const noexcept
{
    return m_player;
}

void Sync::setPlayer(std::optional<Token> value)
{
    m_player = std::move(value);
}

const std::optional<TimeOnly> &Sync::timeOnly() const noexcept
{
    return m_timeOnly;
}

void Sync::setTimeOnly(std::optional<TimeOnly> value)
{
    m_timeOnly = std::move(value);
}

Sync parseSync(pugi::xml_node el)
{
    Sync out;
    bool seen_type = false;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "type")
        {
            seen_type = true;
            out.setType(SyncType::parse(a.value()));
        }
        else if (aname == "latency")
        {
            out.setLatency(Milliseconds::parse(a.value()));
        }
        else if (aname == "player")
        {
            out.setPlayer(Token::parse(a.value()));
        }
        else if (aname == "time-only")
        {
            out.setTimeOnly(TimeOnly::parse(a.value()));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    if (!seen_type)
    {
        throwMissingAttribute(el, "type");
    }
    parseSyncContent(out, el);
    return out;
}

void parseSyncContent(Sync &out, pugi::xml_node el)
{
    (void)out;
    if (firstElement(el))
    {
        throwUnknownElement(firstElement(el));
    }
}

void serializeSync(const Sync &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    setAttribute(el, "type", v.type().toString());
    if (v.latency())
    {
        setAttribute(el, "latency", v.latency()->toString());
    }
    if (v.player())
    {
        setAttribute(el, "player", v.player()->toString());
    }
    if (v.timeOnly())
    {
        setAttribute(el, "time-only", v.timeOnly()->toString());
    }
    serializeSyncContent(v, el);
}

void serializeSyncContent(const Sync &v, pugi::xml_node el)
{
    (void)v;
    (void)el;
}

} // namespace mx::core
