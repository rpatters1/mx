// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/Inversion.h"

#include "mx/core/Lexical.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const std::optional<std::string> &Inversion::text() const noexcept
{
    return m_text;
}

void Inversion::setText(std::optional<std::string> value)
{
    m_text = std::move(value);
}

const std::optional<Tenths> &Inversion::defaultX() const noexcept
{
    return m_defaultX;
}

void Inversion::setDefaultX(std::optional<Tenths> value)
{
    m_defaultX = std::move(value);
}

const std::optional<Tenths> &Inversion::defaultY() const noexcept
{
    return m_defaultY;
}

void Inversion::setDefaultY(std::optional<Tenths> value)
{
    m_defaultY = std::move(value);
}

const std::optional<Tenths> &Inversion::relativeX() const noexcept
{
    return m_relativeX;
}

void Inversion::setRelativeX(std::optional<Tenths> value)
{
    m_relativeX = std::move(value);
}

const std::optional<Tenths> &Inversion::relativeY() const noexcept
{
    return m_relativeY;
}

void Inversion::setRelativeY(std::optional<Tenths> value)
{
    m_relativeY = std::move(value);
}

const std::optional<FontFamily> &Inversion::fontFamily() const noexcept
{
    return m_fontFamily;
}

void Inversion::setFontFamily(std::optional<FontFamily> value)
{
    m_fontFamily = std::move(value);
}

const std::optional<FontStyle> &Inversion::fontStyle() const noexcept
{
    return m_fontStyle;
}

void Inversion::setFontStyle(std::optional<FontStyle> value)
{
    m_fontStyle = std::move(value);
}

const std::optional<FontSize> &Inversion::fontSize() const noexcept
{
    return m_fontSize;
}

void Inversion::setFontSize(std::optional<FontSize> value)
{
    m_fontSize = std::move(value);
}

const std::optional<FontWeight> &Inversion::fontWeight() const noexcept
{
    return m_fontWeight;
}

void Inversion::setFontWeight(std::optional<FontWeight> value)
{
    m_fontWeight = std::move(value);
}

const std::optional<Color> &Inversion::color() const noexcept
{
    return m_color;
}

void Inversion::setColor(std::optional<Color> value)
{
    m_color = std::move(value);
}

const int &Inversion::value() const noexcept
{
    return m_value;
}

void Inversion::setValue(int value)
{
    m_value = std::move(value);
}

Inversion parseInversion(pugi::xml_node el)
{
    Inversion out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "text")
        {
            out.setText(std::string{a.value()});
        }
        else if (aname == "default-x")
        {
            out.setDefaultX(Tenths::parse(a.value()));
        }
        else if (aname == "default-y")
        {
            out.setDefaultY(Tenths::parse(a.value()));
        }
        else if (aname == "relative-x")
        {
            out.setRelativeX(Tenths::parse(a.value()));
        }
        else if (aname == "relative-y")
        {
            out.setRelativeY(Tenths::parse(a.value()));
        }
        else if (aname == "font-family")
        {
            out.setFontFamily(FontFamily::parse(a.value()));
        }
        else if (aname == "font-style")
        {
            out.setFontStyle(FontStyle::parse(a.value()));
        }
        else if (aname == "font-size")
        {
            out.setFontSize(FontSize::parse(a.value()));
        }
        else if (aname == "font-weight")
        {
            out.setFontWeight(FontWeight::parse(a.value()));
        }
        else if (aname == "color")
        {
            out.setColor(Color::parse(a.value()));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    parseInversionContent(out, el);
    return out;
}

void parseInversionContent(Inversion &out, pugi::xml_node el)
{
    out.setValue(parseInt(childText(el)));
    if (firstElement(el))
    {
        throwUnknownElement(firstElement(el));
    }
}

void serializeInversion(const Inversion &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    if (v.text())
    {
        setAttribute(el, "text", *v.text());
    }
    if (v.defaultX())
    {
        setAttribute(el, "default-x", v.defaultX()->toString());
    }
    if (v.defaultY())
    {
        setAttribute(el, "default-y", v.defaultY()->toString());
    }
    if (v.relativeX())
    {
        setAttribute(el, "relative-x", v.relativeX()->toString());
    }
    if (v.relativeY())
    {
        setAttribute(el, "relative-y", v.relativeY()->toString());
    }
    if (v.fontFamily())
    {
        setAttribute(el, "font-family", v.fontFamily()->toString());
    }
    if (v.fontStyle())
    {
        setAttribute(el, "font-style", v.fontStyle()->toString());
    }
    if (v.fontSize())
    {
        setAttribute(el, "font-size", v.fontSize()->toString());
    }
    if (v.fontWeight())
    {
        setAttribute(el, "font-weight", v.fontWeight()->toString());
    }
    if (v.color())
    {
        setAttribute(el, "color", v.color()->toString());
    }
    serializeInversionContent(v, el);
}

void serializeInversionContent(const Inversion &v, pugi::xml_node el)
{
    setText(el, formatInt(v.value()));
}

} // namespace mx::core
