// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/BarStyleColor.h"

#include "mx/core/Lexical.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const std::optional<Color> &BarStyleColor::color() const noexcept
{
    return m_color;
}

void BarStyleColor::setColor(std::optional<Color> value)
{
    m_color = std::move(value);
}

const BarStyle &BarStyleColor::value() const noexcept
{
    return m_value;
}

void BarStyleColor::setValue(BarStyle value)
{
    m_value = std::move(value);
}

BarStyleColor parseBarStyleColor(pugi::xml_node el)
{
    BarStyleColor out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "color")
        {
            out.setColor(Color::parse(a.value()));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    parseBarStyleColorContent(out, el);
    return out;
}

void parseBarStyleColorContent(BarStyleColor &out, pugi::xml_node el)
{
    out.setValue(BarStyle::parse(childText(el)));
    if (firstElement(el))
    {
        throwUnknownElement(firstElement(el));
    }
}

void serializeBarStyleColor(const BarStyleColor &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    if (v.color())
    {
        setAttribute(el, "color", v.color()->toString());
    }
    serializeBarStyleColorContent(v, el);
}

void serializeBarStyleColorContent(const BarStyleColor &v, pugi::xml_node el)
{
    setText(el, v.value().toString());
}

} // namespace mx::core
