// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/LineDetail.h"

#include "mx/core/Lexical.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const StaffLine &LineDetail::line() const noexcept
{
    return m_line;
}

void LineDetail::setLine(StaffLine value)
{
    m_line = std::move(value);
}

const std::optional<Tenths> &LineDetail::width() const noexcept
{
    return m_width;
}

void LineDetail::setWidth(std::optional<Tenths> value)
{
    m_width = std::move(value);
}

const std::optional<Color> &LineDetail::color() const noexcept
{
    return m_color;
}

void LineDetail::setColor(std::optional<Color> value)
{
    m_color = std::move(value);
}

const std::optional<LineType> &LineDetail::lineType() const noexcept
{
    return m_lineType;
}

void LineDetail::setLineType(std::optional<LineType> value)
{
    m_lineType = std::move(value);
}

const std::optional<YesNo> &LineDetail::printObject() const noexcept
{
    return m_printObject;
}

void LineDetail::setPrintObject(std::optional<YesNo> value)
{
    m_printObject = std::move(value);
}

LineDetail parseLineDetail(pugi::xml_node el)
{
    LineDetail out;
    bool seen_line = false;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "line")
        {
            seen_line = true;
            out.setLine(StaffLine::parse(a.value()));
        }
        else if (aname == "width")
        {
            out.setWidth(Tenths::parse(a.value()));
        }
        else if (aname == "color")
        {
            out.setColor(Color::parse(a.value()));
        }
        else if (aname == "line-type")
        {
            out.setLineType(LineType::parse(a.value()));
        }
        else if (aname == "print-object")
        {
            out.setPrintObject(YesNo::parse(a.value()));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    if (!seen_line)
    {
        throwMissingAttribute(el, "line");
    }
    parseLineDetailContent(out, el);
    return out;
}

void parseLineDetailContent(LineDetail &out, pugi::xml_node el)
{
    (void)out;
    if (firstElement(el))
    {
        throwUnknownElement(firstElement(el));
    }
}

void serializeLineDetail(const LineDetail &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    setAttribute(el, "line", v.line().toString());
    if (v.width())
    {
        setAttribute(el, "width", v.width()->toString());
    }
    if (v.color())
    {
        setAttribute(el, "color", v.color()->toString());
    }
    if (v.lineType())
    {
        setAttribute(el, "line-type", v.lineType()->toString());
    }
    if (v.printObject())
    {
        setAttribute(el, "print-object", v.printObject()->toString());
    }
    serializeLineDetailContent(v, el);
}

void serializeLineDetailContent(const LineDetail &v, pugi::xml_node el)
{
    (void)v;
    (void)el;
}

} // namespace mx::core
