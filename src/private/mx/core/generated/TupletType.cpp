// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/TupletType.h"

#include "mx/core/Lexical.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const std::optional<FontFamily> &TupletType::fontFamily() const noexcept
{
    return m_fontFamily;
}

void TupletType::setFontFamily(std::optional<FontFamily> value)
{
    m_fontFamily = std::move(value);
}

const std::optional<FontStyle> &TupletType::fontStyle() const noexcept
{
    return m_fontStyle;
}

void TupletType::setFontStyle(std::optional<FontStyle> value)
{
    m_fontStyle = std::move(value);
}

const std::optional<FontSize> &TupletType::fontSize() const noexcept
{
    return m_fontSize;
}

void TupletType::setFontSize(std::optional<FontSize> value)
{
    m_fontSize = std::move(value);
}

const std::optional<FontWeight> &TupletType::fontWeight() const noexcept
{
    return m_fontWeight;
}

void TupletType::setFontWeight(std::optional<FontWeight> value)
{
    m_fontWeight = std::move(value);
}

const std::optional<Color> &TupletType::color() const noexcept
{
    return m_color;
}

void TupletType::setColor(std::optional<Color> value)
{
    m_color = std::move(value);
}

const NoteTypeValue &TupletType::value() const noexcept
{
    return m_value;
}

void TupletType::setValue(NoteTypeValue value)
{
    m_value = std::move(value);
}

TupletType parseTupletType(pugi::xml_node el)
{
    TupletType out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "font-family")
        {
            out.setFontFamily(FontFamily::parse(a.value()));
        }
        else if (aname == "font-style")
        {
            out.setFontStyle(FontStyle::parse(a.value()));
        }
        else if (aname == "font-size")
        {
            out.setFontSize(FontSize::parse(a.value()));
        }
        else if (aname == "font-weight")
        {
            out.setFontWeight(FontWeight::parse(a.value()));
        }
        else if (aname == "color")
        {
            out.setColor(Color::parse(a.value()));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    parseTupletTypeContent(out, el);
    return out;
}

void parseTupletTypeContent(TupletType &out, pugi::xml_node el)
{
    out.setValue(NoteTypeValue::parse(childText(el)));
    if (firstElement(el))
    {
        throwUnknownElement(firstElement(el));
    }
}

void serializeTupletType(const TupletType &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    if (v.fontFamily())
    {
        setAttribute(el, "font-family", v.fontFamily()->toString());
    }
    if (v.fontStyle())
    {
        setAttribute(el, "font-style", v.fontStyle()->toString());
    }
    if (v.fontSize())
    {
        setAttribute(el, "font-size", v.fontSize()->toString());
    }
    if (v.fontWeight())
    {
        setAttribute(el, "font-weight", v.fontWeight()->toString());
    }
    if (v.color())
    {
        setAttribute(el, "color", v.color()->toString());
    }
    serializeTupletTypeContent(v, el);
}

void serializeTupletTypeContent(const TupletType &v, pugi::xml_node el)
{
    setText(el, v.value().toString());
}

} // namespace mx::core
