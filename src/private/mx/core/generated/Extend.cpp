// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/Extend.h"

#include "mx/core/Lexical.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const std::optional<StartStopContinue> &Extend::type() const noexcept
{
    return m_type;
}

void Extend::setType(std::optional<StartStopContinue> value)
{
    m_type = std::move(value);
}

const std::optional<Tenths> &Extend::defaultX() const noexcept
{
    return m_defaultX;
}

void Extend::setDefaultX(std::optional<Tenths> value)
{
    m_defaultX = std::move(value);
}

const std::optional<Tenths> &Extend::defaultY() const noexcept
{
    return m_defaultY;
}

void Extend::setDefaultY(std::optional<Tenths> value)
{
    m_defaultY = std::move(value);
}

const std::optional<Tenths> &Extend::relativeX() const noexcept
{
    return m_relativeX;
}

void Extend::setRelativeX(std::optional<Tenths> value)
{
    m_relativeX = std::move(value);
}

const std::optional<Tenths> &Extend::relativeY() const noexcept
{
    return m_relativeY;
}

void Extend::setRelativeY(std::optional<Tenths> value)
{
    m_relativeY = std::move(value);
}

const std::optional<Color> &Extend::color() const noexcept
{
    return m_color;
}

void Extend::setColor(std::optional<Color> value)
{
    m_color = std::move(value);
}

Extend parseExtend(pugi::xml_node el)
{
    Extend out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "type")
        {
            out.setType(StartStopContinue::parse(a.value()));
        }
        else if (aname == "default-x")
        {
            out.setDefaultX(Tenths::parse(a.value()));
        }
        else if (aname == "default-y")
        {
            out.setDefaultY(Tenths::parse(a.value()));
        }
        else if (aname == "relative-x")
        {
            out.setRelativeX(Tenths::parse(a.value()));
        }
        else if (aname == "relative-y")
        {
            out.setRelativeY(Tenths::parse(a.value()));
        }
        else if (aname == "color")
        {
            out.setColor(Color::parse(a.value()));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    parseExtendContent(out, el);
    return out;
}

void parseExtendContent(Extend &out, pugi::xml_node el)
{
    (void)out;
    if (firstElement(el))
    {
        throwUnknownElement(firstElement(el));
    }
}

void serializeExtend(const Extend &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    if (v.type())
    {
        setAttribute(el, "type", v.type()->toString());
    }
    if (v.defaultX())
    {
        setAttribute(el, "default-x", v.defaultX()->toString());
    }
    if (v.defaultY())
    {
        setAttribute(el, "default-y", v.defaultY()->toString());
    }
    if (v.relativeX())
    {
        setAttribute(el, "relative-x", v.relativeX()->toString());
    }
    if (v.relativeY())
    {
        setAttribute(el, "relative-y", v.relativeY()->toString());
    }
    if (v.color())
    {
        setAttribute(el, "color", v.color()->toString());
    }
    serializeExtendContent(v, el);
}

void serializeExtendContent(const Extend &v, pugi::xml_node el)
{
    (void)v;
    (void)el;
}

} // namespace mx::core
