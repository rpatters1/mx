// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/Repeat.h"

#include "mx/core/Lexical.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const BackwardForward &Repeat::direction() const noexcept
{
    return m_direction;
}

void Repeat::setDirection(BackwardForward value)
{
    m_direction = std::move(value);
}

const std::optional<int> &Repeat::times() const noexcept
{
    return m_times;
}

void Repeat::setTimes(std::optional<int> value)
{
    m_times = std::move(value);
}

const std::optional<YesNo> &Repeat::afterJump() const noexcept
{
    return m_afterJump;
}

void Repeat::setAfterJump(std::optional<YesNo> value)
{
    m_afterJump = std::move(value);
}

const std::optional<Winged> &Repeat::winged() const noexcept
{
    return m_winged;
}

void Repeat::setWinged(std::optional<Winged> value)
{
    m_winged = std::move(value);
}

Repeat parseRepeat(pugi::xml_node el)
{
    Repeat out;
    bool seen_direction = false;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "direction")
        {
            seen_direction = true;
            out.setDirection(BackwardForward::parse(a.value()));
        }
        else if (aname == "times")
        {
            out.setTimes(parseInt(a.value()));
        }
        else if (aname == "after-jump")
        {
            out.setAfterJump(YesNo::parse(a.value()));
        }
        else if (aname == "winged")
        {
            out.setWinged(Winged::parse(a.value()));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    if (!seen_direction)
    {
        throwMissingAttribute(el, "direction");
    }
    parseRepeatContent(out, el);
    return out;
}

void parseRepeatContent(Repeat &out, pugi::xml_node el)
{
    (void)out;
    if (firstElement(el))
    {
        throwUnknownElement(firstElement(el));
    }
}

void serializeRepeat(const Repeat &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    setAttribute(el, "direction", v.direction().toString());
    if (v.times())
    {
        setAttribute(el, "times", formatInt(*v.times()));
    }
    if (v.afterJump())
    {
        setAttribute(el, "after-jump", v.afterJump()->toString());
    }
    if (v.winged())
    {
        setAttribute(el, "winged", v.winged()->toString());
    }
    serializeRepeatContent(v, el);
}

void serializeRepeatContent(const Repeat &v, pugi::xml_node el)
{
    (void)v;
    (void)el;
}

} // namespace mx::core
