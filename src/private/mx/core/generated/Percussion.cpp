// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/Percussion.h"

#include "mx/core/Lexical.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const std::optional<Tenths> &Percussion::defaultX() const noexcept
{
    return m_defaultX;
}

void Percussion::setDefaultX(std::optional<Tenths> value)
{
    m_defaultX = std::move(value);
}

const std::optional<Tenths> &Percussion::defaultY() const noexcept
{
    return m_defaultY;
}

void Percussion::setDefaultY(std::optional<Tenths> value)
{
    m_defaultY = std::move(value);
}

const std::optional<Tenths> &Percussion::relativeX() const noexcept
{
    return m_relativeX;
}

void Percussion::setRelativeX(std::optional<Tenths> value)
{
    m_relativeX = std::move(value);
}

const std::optional<Tenths> &Percussion::relativeY() const noexcept
{
    return m_relativeY;
}

void Percussion::setRelativeY(std::optional<Tenths> value)
{
    m_relativeY = std::move(value);
}

const std::optional<FontFamily> &Percussion::fontFamily() const noexcept
{
    return m_fontFamily;
}

void Percussion::setFontFamily(std::optional<FontFamily> value)
{
    m_fontFamily = std::move(value);
}

const std::optional<FontStyle> &Percussion::fontStyle() const noexcept
{
    return m_fontStyle;
}

void Percussion::setFontStyle(std::optional<FontStyle> value)
{
    m_fontStyle = std::move(value);
}

const std::optional<FontSize> &Percussion::fontSize() const noexcept
{
    return m_fontSize;
}

void Percussion::setFontSize(std::optional<FontSize> value)
{
    m_fontSize = std::move(value);
}

const std::optional<FontWeight> &Percussion::fontWeight() const noexcept
{
    return m_fontWeight;
}

void Percussion::setFontWeight(std::optional<FontWeight> value)
{
    m_fontWeight = std::move(value);
}

const std::optional<Color> &Percussion::color() const noexcept
{
    return m_color;
}

void Percussion::setColor(std::optional<Color> value)
{
    m_color = std::move(value);
}

const std::optional<LeftCenterRight> &Percussion::halign() const noexcept
{
    return m_halign;
}

void Percussion::setHalign(std::optional<LeftCenterRight> value)
{
    m_halign = std::move(value);
}

const std::optional<Valign> &Percussion::valign() const noexcept
{
    return m_valign;
}

void Percussion::setValign(std::optional<Valign> value)
{
    m_valign = std::move(value);
}

const std::optional<EnclosureShape> &Percussion::enclosure() const noexcept
{
    return m_enclosure;
}

void Percussion::setEnclosure(std::optional<EnclosureShape> value)
{
    m_enclosure = std::move(value);
}

const std::optional<Token> &Percussion::id() const noexcept
{
    return m_id;
}

void Percussion::setID(std::optional<Token> value)
{
    m_id = std::move(value);
}

const PercussionChoice &Percussion::choice() const noexcept
{
    return m_choice;
}

void Percussion::setChoice(PercussionChoice value)
{
    m_choice = std::move(value);
}

Percussion parsePercussion(pugi::xml_node el)
{
    Percussion out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "default-x")
        {
            out.setDefaultX(Tenths::parse(a.value()));
        }
        else if (aname == "default-y")
        {
            out.setDefaultY(Tenths::parse(a.value()));
        }
        else if (aname == "relative-x")
        {
            out.setRelativeX(Tenths::parse(a.value()));
        }
        else if (aname == "relative-y")
        {
            out.setRelativeY(Tenths::parse(a.value()));
        }
        else if (aname == "font-family")
        {
            out.setFontFamily(FontFamily::parse(a.value()));
        }
        else if (aname == "font-style")
        {
            out.setFontStyle(FontStyle::parse(a.value()));
        }
        else if (aname == "font-size")
        {
            out.setFontSize(FontSize::parse(a.value()));
        }
        else if (aname == "font-weight")
        {
            out.setFontWeight(FontWeight::parse(a.value()));
        }
        else if (aname == "color")
        {
            out.setColor(Color::parse(a.value()));
        }
        else if (aname == "halign")
        {
            out.setHalign(LeftCenterRight::parse(a.value()));
        }
        else if (aname == "valign")
        {
            out.setValign(Valign::parse(a.value()));
        }
        else if (aname == "enclosure")
        {
            out.setEnclosure(EnclosureShape::parse(a.value()));
        }
        else if (aname == "id")
        {
            out.setID(Token::parse(a.value()));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    parsePercussionContent(out, el);
    return out;
}

void parsePercussionContent(Percussion &out, pugi::xml_node el)
{
    pugi::xml_node cursor = firstElement(el);
    if (cursor && (cursorIs(cursor, "glass") || cursorIs(cursor, "metal") || cursorIs(cursor, "wood") ||
                   cursorIs(cursor, "pitched") || cursorIs(cursor, "membrane") || cursorIs(cursor, "effect") ||
                   cursorIs(cursor, "timpani") || cursorIs(cursor, "beater") || cursorIs(cursor, "stick") ||
                   cursorIs(cursor, "stick-location") || cursorIs(cursor, "other-percussion")))
    {
        out.setChoice(parsePercussionChoice(el, cursor));
    }
    else
    {
        throwMissingElement(el, "glass");
    }
    if (cursor)
    {
        if (cursorIs(cursor, "glass") || cursorIs(cursor, "metal") || cursorIs(cursor, "wood") ||
            cursorIs(cursor, "pitched") || cursorIs(cursor, "membrane") || cursorIs(cursor, "effect") ||
            cursorIs(cursor, "timpani") || cursorIs(cursor, "beater") || cursorIs(cursor, "stick") ||
            cursorIs(cursor, "stick-location") || cursorIs(cursor, "other-percussion"))
        {
            throwMisplacedElement(cursor);
        }
        throwUnknownElement(cursor);
    }
}

void serializePercussion(const Percussion &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    if (v.defaultX())
    {
        setAttribute(el, "default-x", v.defaultX()->toString());
    }
    if (v.defaultY())
    {
        setAttribute(el, "default-y", v.defaultY()->toString());
    }
    if (v.relativeX())
    {
        setAttribute(el, "relative-x", v.relativeX()->toString());
    }
    if (v.relativeY())
    {
        setAttribute(el, "relative-y", v.relativeY()->toString());
    }
    if (v.fontFamily())
    {
        setAttribute(el, "font-family", v.fontFamily()->toString());
    }
    if (v.fontStyle())
    {
        setAttribute(el, "font-style", v.fontStyle()->toString());
    }
    if (v.fontSize())
    {
        setAttribute(el, "font-size", v.fontSize()->toString());
    }
    if (v.fontWeight())
    {
        setAttribute(el, "font-weight", v.fontWeight()->toString());
    }
    if (v.color())
    {
        setAttribute(el, "color", v.color()->toString());
    }
    if (v.halign())
    {
        setAttribute(el, "halign", v.halign()->toString());
    }
    if (v.valign())
    {
        setAttribute(el, "valign", v.valign()->toString());
    }
    if (v.enclosure())
    {
        setAttribute(el, "enclosure", v.enclosure()->toString());
    }
    if (v.id())
    {
        setAttribute(el, "id", v.id()->toString());
    }
    serializePercussionContent(v, el);
}

void serializePercussionContent(const Percussion &v, pugi::xml_node el)
{
    serializePercussionChoice(v.choice(), el);
}

} // namespace mx::core
