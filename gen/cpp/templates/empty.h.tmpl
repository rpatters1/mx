// {{generated_banner}}

#pragma once

namespace pugi
{
class xml_node;
}

namespace {{vars.namespace}}
{

{{#doc_lines}}
/// {{value}}
{{/doc_lines}}
/// Presence-only marker: the element's only information is whether it
/// appears. Optional occurrences project to bool fields; choice
/// alternatives hold this marker (plan §2.3).
class {{ident}}{{^is_base}} final{{/is_base}}
{
public:
    bool operator==(const {{ident}} &) const noexcept = default;
};

{{ident}} parse{{ident}}(pugi::xml_node el);

void parse{{ident}}Content({{ident}} &out, pugi::xml_node el);

void serialize{{ident}}(const {{ident}} &v, pugi::xml_node parent, const char *tag);

void serialize{{ident}}Content(const {{ident}} &v, pugi::xml_node el);

} // namespace {{vars.namespace}}
